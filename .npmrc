# Fail `npm install` on an unsupported Node rather than letting it succeed and
# break at runtime. The engines range in package.json is >=22.12.0; without
# this, npm treats it as advisory and a friend on Node 20 gets a confusing
# runtime error instead of a readable one at install time.
engine-strict=true
